# Hours of content to look back (default: 24)
LOOKBACK_HOURS=24

# Ollama model for summarization, tagging, deduplication, and classification
# Use a lightweight model here (e.g. llama3.2, phi4-mini, gemma3:4b)
#SUMMARIZE_MODEL=llama3.1:8b
SUMMARIZE_MODEL=llama3.2:3b
#SUMMARIZE_MODEL=gemma4:e4b

# Ollama model for ranking items by relevance
# Use a larger model here for better instruction following (e.g. qwen3.5:9b, qwen3.6:35b-a3b)
RANK_MODEL=qwen3.5:9b

# Ollama model for final HTML generation orchestration
# Use a strong/thinking model here (e.g. qwen3, deepseek-r1, llama3.3)
GENERATE_MODEL=qwen3.5:35b-a3b

# Gmail OAuth2 credentials are loaded from ~/keys/kfopenclaw-gmail.env (outside the repo).
# Create that file with:
#   GMAIL_CLIENT_ID=your-client-id.apps.googleusercontent.com
#   GMAIL_CLIENT_SECRET=your-client-secret
#
# Alternatively, point to a downloaded credentials.json file:
# GMAIL_CREDENTIALS=credentials.json

# Path to Gmail OAuth2 token file (auto-created on first run, do not edit)
GMAIL_TOKEN=token.json

# Parallelism
# LLM_WORKERS controls how many summarization calls run at once.
# Set OLLAMA_NUM_PARALLEL to the same value so Ollama actually processes them concurrently:
#   OLLAMA_NUM_PARALLEL=2 ollama serve   (or set in /etc/systemd or launchd plist)
LLM_WORKERS=2
# URL_WORKERS controls parallel HTTP fetches for email links (I/O bound, safe to set higher)
URL_WORKERS=10
