[project]
name = "techradar-agent-consumer"
version = "0.1.0"
description = "web-pages' AI/Robotics digest — thin consumer of the news-radar package"
requires-python = ">=3.13"
dependencies = [
    "newsradar",
    "python-dotenv",
]

[tool.uv]
package = false

[tool.uv.sources]
newsradar = { path = "../../../news-radar", editable = true }
